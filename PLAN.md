# MailPulse 项目计划

## 1. 项目定位

MailPulse 面向公司内网，提供从邮件同步、规则筛选、附件理解到报告生成和投递的完整流程。系统采用本地账号和角色权限，支持内部模型服务，也支持 OpenAI-compatible 接口。

当前代码已完成可运行的核心垂直切片，后续工作重点是生产化加固、运维能力和配置体验。

## 2. 产品范围

### 管理控制台

- 系统概览：账号数量、模型配置和最近任务状态。
- 用户管理：创建账号、查看账号和启用/停用账号。
- 模型管理：维护 Primary/Vision 模型、能力声明和运行策略。
- 任务监控：查看同步、归纳和投递任务的状态、时间与错误信息。
- 账号设置：修改管理员登录密码；默认管理员首次登录时提供修改或跳过入口。

### 邮件工作台

- 任务管理：每个任务包含一个收件邮箱（IMAP 连接与 SMTP 发信身份、连接验证）、一系列筛选规则和若干报告投递渠道。
- 邮件同步、搜索（范围为所有任务邮箱同步的全部邮件，可按邮箱筛选）、标签、星标和已处理状态。
- 任务执行：手动运行（完整流水线）与定时运行（每日/每周/自定义 cron，IANA 时区）。
- 报告生成、网页查看（始终开启）、SMTP 投递和失败重试。
- 账号设置：修改密码与显示名称。

## 3. 系统架构

```text
IMAP 邮箱
   ↓ 只读同步
CanonicalMessage / MessageOccurrence
   ↓ 规则筛选
附件按需下载 → MarkItDown → Markdown 和图片资源
   ↓ 模型编排
Primary 模型 ← Visual Evidence ← 可选 Vision 模型
   ↓
结构化报告 → 网页查看 / SMTP 投递
```

核心模块：

- `mail`：IMAP 连接、增量同步、游标和邮件规范化。
- `filtering` / `rules`：规则 DSL、验证和安全求值。
- `attachments`：附件限制、MarkItDown 转换和资源管理。
- `ai`：模型目录、能力声明、模型路由和结构化输出。
- `reports`：报告生成、Markdown 渲染和来源引用。
- `delivery`：SMTP 投递、失败记录和重试。
- `search`：全文索引和组合筛选。
- `worker`：任务定时运行、运行记录和失败恢复。
- `web`：Session 认证、CSRF、权限依赖和网页路由。

## 4. AI 与附件处理

模型不接收原生 PDF 或 Office 文件。所有进入模型流程的附件先经过项目内置 MarkItDown，形成 Markdown 文本和关联图片资源。

支持两种主要编排方式：

1. 多模态 Primary 模型直接处理邮件正文、Markdown 和图片资源。
2. 文本 Primary 模型调用 Vision 模型提取视觉证据，再由 Primary 模型生成最终报告。

Vision 模型输出的是可校验的视觉证据，不直接替代最终报告。模型输入受到字符数、token、图片数量、图片大小、超时和重试次数限制。

报告应包含摘要、分类、优先级、行动项、风险、待确认事项和来源定位。模型无法验证的内容应标记为未验证，不应补造结论。

## 5. 权限模型

| 角色 | 允许访问 |
| --- | --- |
| `user` | 当前账号的邮箱、邮件、规则、任务、报告和投递记录 |
| `admin` | 账号、模型配置、任务运行记录和平台概览 |

登录后的首页和导航由角色决定。所有路由在服务端执行角色检查，所有用户数据查询同时带账号范围。

## 6. 数据与安全原则

- 邮箱默认只读，不修改 IMAP 服务器状态。
- Session 使用签名 Cookie，生产 HTTPS 部署可启用 Secure 属性；表单使用 CSRF 防护，登录失败使用限流。
- 未登录访问受保护页面时跳转到登录页；登录状态可以选择保持浏览器会话或按配置延长有效期，系统不保存密码。
- 邮箱凭据和 AI API Key 使用独立密钥加密保存。
- 邮件正文、HTML、附件和模型输出均视为不可信输入。
- 禁止把邮件内容作为工具指令，禁止任意代码执行。
- 附件同步和转换受单文件大小、单封邮件附件数量、用户/全局存储配额、图片资源数量与大小等限制。
- 运行日志不得包含密码、API Key、邮件正文或附件内容。

## 7. 当前实现状态

已完成：

- FastAPI/Jinja2 网页应用和角色专属页面。
- 本地账号、Session、CSRF、登录限流和服务端权限校验。
- IMAP 只读增量同步、UIDVALIDITY 处理和邮件去重。
- SQLite/WAL、SQLAlchemy、Alembic 和 CLI 运行入口。
- 规则 DSL、规则求值、全文搜索和本地邮件状态。
- MarkItDown 附件转换及 Markdown/图片资源处理。
- Primary/Vision 模型编排和 OpenAI-compatible Provider。
- 任务运行流水线（手动/定时）、JobRun、SMTP 投递、失败重试和报告页面。
- Fake Provider 测试、权限测试和网页流程测试。
- 服务首次启动自动按模型创建数据库表结构，并创建默认管理员账号；开发阶段不维护迁移文件，模型变更后通过 `reset-db` 重置本地数据库。
- 任务中心化用户界面：侧边栏框架、任务列表与任务详情（基本信息、收件邮箱、筛选规则、投递渠道、运行记录、历史报告），支持手动与定时两种执行方式。
- 新建任务四步向导：基本信息 → 收件邮箱（IMAP 即时连接验证、SMTP 可选）→ 筛选规则（表单化编辑 + JSON 高级模式）→ 投递渠道（网页查看始终开启，邮件地址增删改、启停与测试邮件）。
- 筛选规则 OR 语义：任一启用规则命中即纳入报告，规则列表按优先级排序（上移/下移）；求值器新增 `not_contains` 操作符。
- 邮件全局搜索页覆盖所有任务邮箱同步的全部邮件，支持按邮箱筛选。
- 普通用户账号设置（修改密码、显示名称），管理员账号设置保持独立。
- 默认管理员首次登录进入独立密码设置页面，支持修改或暂时跳过。
- 提供幂等的 `init-db` 和需显式确认的 `reset-db --confirm` CLI 命令。
- 登录页支持记住登录状态；普通登录使用浏览器会话 Cookie，勾选后默认保持 30 天且不保存密码。
- 应用和后台组件统一使用 Loguru，文件日志按天轮转并支持保留策略。
- `serve` 启动时输出实际监听地址，以及 host/port 来自命令行、环境变量、`.env` 或代码默认值的信息。

当前验证命令：

```bash
uv run pytest
uv run ruff check .
uv run python -m mailpulse --help
```

## 8. 后续计划

### 优先级 P0：生产化基础

- 在目标环境完成真实 IMAP、SMTP 和 AI 服务验收。
- 增加用户密码重置、账号生命周期和更完整的管理员操作记录。
- 完善 worker 单实例锁、异常恢复和重复运行保护。
- 增加附件配额清理、存储监控和可恢复清理流程。

### 优先级 P1：配置与使用体验

- 扩展首次使用配置向导和配置状态检查。
- 模型连接测试、能力探测和更清晰的运行诊断。
- 可复用的规则模板和报告模板。
- 报告导出和更多投递渠道。

### 优先级 P2：规模与集成

- 多邮箱和多文件夹同步。
- 在 SQLite/WAL 方案边界内完善存储、备份和恢复能力；进入需要保留数据的阶段后，再引入版本化数据库迁移。
- SSO/OAuth2 集成。
- 更细粒度的审计查询和运维指标。

## 9. 验收标准

1. 普通用户可以独立完成邮箱配置、同步、筛选和报告查看。
2. 管理员可以完成账号、模型和任务运行管理。
3. 管理员与普通用户的路由和导航相互隔离。
4. 多模态主模型和文本主模型搭配视觉模型的流程均可验证。
5. 附件经过 MarkItDown 转换后再进入模型编排。
6. 报告来源可以定位到邮件、附件、页码或图片资源。
7. 模型或附件处理失败时，系统返回明确状态，不生成未经验证的内容。
8. 测试和静态检查在干净环境中通过。
9. 空数据库首次启动可以自动创建默认管理员，后续启动不会重复创建或打印密码。
10. 受保护入口对未登录请求跳转登录页，管理员和普通用户分别进入对应工作区。
11. 登录会话策略、日志轮转和敏感信息过滤符合配置与安全要求。
